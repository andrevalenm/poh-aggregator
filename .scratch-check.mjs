import { chromium } from '@playwright/test'
const base = '/tmp/claude-1000/-home-hugo-Projects/f89858e5-2ca1-420d-9d70-24112d525bae/scratchpad'
const browser = await chromium.launch({ headless: false })
const page = await browser.newPage({ viewport: { width: 1440, height: 900 } })
await page.goto('http://localhost:4188/', { waitUntil: 'networkidle' })
await page.waitForTimeout(2800)
await page.screenshot({ path: `${base}/chk-hero.png` })
await page.evaluate(() => document.querySelectorAll('.reveal').forEach((el) => el.classList.add('in')))
await page.locator('.install').scrollIntoViewIfNeeded()
await page.evaluate(() => scrollBy(0, -160))
await page.waitForTimeout(700)
await page.screenshot({ path: `${base}/chk-tear.png` })
await page.locator('.colophon-fine').scrollIntoViewIfNeeded()
await page.waitForTimeout(600)
await page.screenshot({ path: `${base}/chk-footer.png` })
await browser.close()
console.log('done')
