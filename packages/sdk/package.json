{
  "name": "@corroborate/sdk",
  "version": "0.1.0",
  "description": "Aggregate proof-of-personhood credentials across protocols, scored by independent trust root.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "cp ../../ontology/adapters.json src/ontology-data.json && tsc -p tsconfig.json && cp src/ontology-data.json dist/ && node scripts/fix-dts.mjs",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts'",
    "test:live": "LIVE=1 node --test --experimental-strip-types 'src/**/*.live.test.ts'"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}